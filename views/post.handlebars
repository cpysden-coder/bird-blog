<div class="container text-center">
  <h2 class="post-h1">{{title}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  
  
</div>
{{!-- {{log post.comments}}
{{#if post.comments}}
  <h3>Comments</h3>
  {{#each post.comments}}
  <div>
    <p>{{body}}</p>
    <p>Created by {{user.name}}</p>
    
  </div>
{{/if}} --}}
